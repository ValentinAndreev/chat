h4 All messages
active[data-chatroom=@chatroom.id]
  = render @messages
- if joined?(current_user, @chatroom)
  = form_for [@chatroom, @message] do |f|
    = f.label :text
    = f.text_field :text, class: "form-control", id: 'text_field', autofocus: true
= link_to 'Back', root_path